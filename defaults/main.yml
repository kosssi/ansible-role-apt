---

apt_cache_valid_time: 3600
apt_update: true

apt_upgrade: true
apt_upgrade_type: safe          # safe, full or dist

apt_dependencies:
  - python-apt
  - aptitude

apt_install:
  - python-apt
  - unattended-upgrades

apt_repositories: false
# apt_repositories:
#   - ppa:ondrej/php5

apt_remove: false
